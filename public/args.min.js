const argTag = document.querySelectorAll("arg")
const importantTag = document.querySelectorAll("important")

const argFormat = 
    "font-family: Sans-serif;"

argTag.setAttribute("style", argFormat)

const importantFormat = 
    "font-family: Sans-serif;"+
    "color: red;"

importantTag.setAttribute("style", importantFormat)